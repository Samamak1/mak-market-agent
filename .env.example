# OPTIONAL. The agent is fully functional without any API key:
# retrieval is local and the default answer is an extractive summary.
# Set at most one of these ONLY to enable LLM answer synthesis via
# use_llm=true / --use-llm. Never commit a real key.
ANTHROPIC_API_KEY=
GROQ_API_KEY=
